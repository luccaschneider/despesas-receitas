# Produção: ajustes de comportamento (sem credenciais — use DB_URL, DB_USER, DB_PASSWORD).
spring.jpa.show-sql=false
spring.jpa.hibernate.ddl-auto=validate
spring.thymeleaf.cache=true
logging.level.root=WARN
logging.level.com.univates.despesasreceitas=INFO
