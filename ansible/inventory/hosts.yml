---
# Inventario padrao: deploy na propria VM (bootstrap, deploy_*.sh e painel na VM).
# Para dev local com tunel SSH, use inventory/hosts-dev.yml
all:
  children:
    homolog:
      hosts:
        vm:
          ansible_connection: local
    prod:
      hosts:
        vm:
          ansible_connection: local
